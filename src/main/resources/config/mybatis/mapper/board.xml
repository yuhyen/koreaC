<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">

	<select id="selectList" resultType="hashMap">
		SELECT * FROM TB_BOARD_BASE
	</select>
		
	<select id="selectOne">
		SELECT * FROM TB_BOARD_BASE
		WHERE ROWNUM = 1

	</select>
	
	<insert id="insertBase">
		INSERT INTO TB_BOARD_BASE
		(B_SEQ, B_CATEGORY, B_USER, B_NOTICE_YN, B_IMG_PATH, B_TITLE, B_CRTR, B_REPLY, B_DEEP, B_REG_DATE, B_REG_USER, B_MOD_DATE, B_MOD_USER) 
			VALUES 
		( ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ? );				
	</insert>
	
	<insert id="insertDtls">
		
	</insert>
	
	<update id="updateBase">
	
	</update>
	
	<update id="updateDtls">
	
	</update>
	
	
</mapper>












